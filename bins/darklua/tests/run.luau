local toolchainlib = require("../lune_packages/core")

return function(version)
	-- local exit_code = toolchainlib("seaofvoices/darklua", "bins/darklua", { "bins/darklua/tests/input" })
	-- assert(exit_code == 0, `shouldnt error`)
end
